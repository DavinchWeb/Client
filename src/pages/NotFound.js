import React from "react";

const NotFound = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyItems: "center",
        fontSize: "64px",
        position: "absolute",
        left: "50%",
        top: "50%",
      }}
    >
      404
    </div>
  );
};

export default NotFound;
